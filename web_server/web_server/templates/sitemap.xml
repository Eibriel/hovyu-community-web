<?xml version="1.0" encoding="UTF-8"?>
<urlset xmlns="http://www.sitemaps.org/schemas/sitemap/0.9"> 
  <url>
    <loc>{{ canonical_domain}}/about</loc> 
  </url>
  {% for store in stores %}
  <url>
    <loc>{{ canonical_domain}}/?store={{ store['_id'] }}</loc> 
  </url>
  {% endfor %}

  {% for product in products %}
  <url>
    <loc>{{ canonical_domain}}/?latitude=&amp;longitude=&amp;product={{ product['_id'] }}&amp;activity=</loc> 
  </url>
  {% endfor %}

  {% for activity in activities %}
  <url>
    <loc>{{ canonical_domain}}/?latitude=&amp;longitude=&amp;product=&amp;activity={{ activity['_id'] }}</loc> 
  </url>
  {% endfor %}
</urlset>
